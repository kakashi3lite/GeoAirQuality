# Data

| Path | Contents | Tracked? |
|------|----------|----------|
| `raw/` | Source CSVs (generated by pipeline) | no |
| `processed/` | Processed outputs (timestamps) | no |
| `cache/` | Pipeline cache | no |

> The canonical **sample datasets** (`AirQuality.csv`,
> `GlobalWeatherRepository.csv`) live at the repo root — the research
> notebook fetches them by that stable path.
